# Model Assets

Place exported mobile models here during development, for example:

- `vehicle_detector.onnx`
- `vehicle_detector.tflite`

Do not commit large trained model binaries unless that is explicitly desired for your workflow.
